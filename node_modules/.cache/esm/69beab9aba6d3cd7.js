let calculateTax;_ba6‍.x([["printDetails",()=>printDetails],["applyDiscount",()=>applyDiscount]]);_ba6‍.w("./tax",[["calculateTax",["calculateTax"],function(v){calculateTax=v}]]);
       function printDetails(product) {
  let taxedPrice = calculateTax(product.price);
  console.log(`Name: ${product.name}, Taxed Price: $${taxedPrice.toFixed(2)}`);
}

       function applyDiscount(product, discount = 5) {
  product.price = product.price - discount;
}
